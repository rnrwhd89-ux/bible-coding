# 📱 핸드폰 테스트 가이드

바이브 앱을 핸드폰에서 테스트하기 위한 완전 가이드입니다.

## 🎯 현재 상태

✅ **완료된 작업:**
- Firebase 프로젝트 설정 완료
- 멀티챗 기능 구현 완료
- 프로덕션 빌드 생성 완료
- Firebase 호스팅 설정 완료
- Firestore 보안 규칙 준비 완료

## 🚀 배포 방법 (3단계)

### 1단계: Firebase 로그인

터미널에서 다음 명령어를 실행하세요:

```bash
cd /Users/kkuk/Desktop/바이브코딩/bible-app
npx firebase login
```

- 브라우저가 자동으로 열립니다
- Google 계정으로 로그인하세요
- 로그인 완료 후 터미널로 돌아옵니다

### 2단계: 배포 실행

**방법 1 - 간편 배포 (추천):**
```bash
./deploy.sh
```

**방법 2 - npm 스크립트:**
```bash
npm run deploy
```

**방법 3 - 수동 배포:**
```bash
npm run build
npx firebase deploy
```

### 3단계: 핸드폰에서 접속

배포가 완료되면 다음 URL이 표시됩니다:

**배포 URL: https://bible-app-29a53.web.app**

핸드폰 브라우저에서 이 URL을 열면 앱을 바로 사용할 수 있습니다!

## 📋 체크리스트

배포 전 확인사항:

- [ ] Firebase CLI 로그인 완료
- [ ] 빌드 에러 없음
- [ ] Firebase 콘솔에서 Authentication > Google 로그인 활성화
- [ ] Firestore Database 생성됨
- [ ] Firestore 보안 규칙 배포됨

## 🔧 Firebase 콘솔 설정

### 1. Authentication 설정

1. Firebase 콘솔 접속: https://console.firebase.google.com/project/bible-app-29a53
2. **Authentication** 메뉴 클릭
3. **Sign-in method** 탭 클릭
4. **Google** 선택하고 **사용 설정** 클릭
5. 프로젝트 지원 이메일 선택
6. 저장

### 2. Firestore Database 생성 (아직 안 했다면)

1. Firebase 콘솔에서 **Firestore Database** 클릭
2. **데이터베이스 만들기** 클릭
3. **프로덕션 모드로 시작** 선택
4. 위치: `asia-northeast3 (Seoul)` 선택 (가장 가까운 지역)
5. 사용 설정

### 3. Firestore 보안 규칙 확인

배포 시 자동으로 설정되지만, 수동 확인 방법:

1. Firestore Database → **규칙** 탭
2. 다음 규칙이 적용되어 있는지 확인:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /chatRooms/{roomId} {
      allow read, create, update: if request.auth != null;
      match /messages/{messageId} {
        allow read, create: if request.auth != null;
      }
    }
  }
}
```

## 📱 핸드폰에서 사용하기

### 일반 브라우저 접속
1. Chrome, Safari 등 브라우저 열기
2. https://bible-app-29a53.web.app 입력
3. 바로 사용 가능!

### PWA로 설치하기 (앱처럼 사용)

**Android (Chrome):**
1. 앱 접속
2. 우측 상단 메뉴(⋮) → "홈 화면에 추가"
3. "추가" 클릭

**iPhone (Safari):**
1. 앱 접속
2. 하단 공유 버튼(□↑) 클릭
3. "홈 화면에 추가" 선택
4. "추가" 클릭

## 🎮 테스트 시나리오

### 1. 로그인 테스트
- [ ] 채팅 탭 클릭
- [ ] Google 로그인 버튼 클릭
- [ ] Google 계정 선택
- [ ] 로그인 성공 확인

### 2. 채팅방 생성 테스트
- [ ] "+" 버튼 클릭
- [ ] 채팅방 이름 입력 (예: "테스트 채팅방")
- [ ] "생성" 버튼 클릭
- [ ] 채팅방이 목록에 표시되는지 확인

### 3. 메시지 송수신 테스트
- [ ] 생성한 채팅방 선택
- [ ] 메시지 입력 (예: "안녕하세요!")
- [ ] "전송" 버튼 클릭
- [ ] 메시지가 표시되는지 확인

### 4. 다중 기기 동기화 테스트
- [ ] 컴퓨터와 핸드폰에서 동시 접속
- [ ] 한쪽에서 메시지 전송
- [ ] 다른 쪽에서 실시간으로 메시지 수신 확인

## 🔍 문제 해결

### 로그인 오류
```bash
npx firebase login --reauth
```

### 배포 실패
```bash
npx firebase deploy --debug
```

### 빌드 에러
```bash
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Firebase 콘솔 확인
- 호스팅 상태: https://console.firebase.google.com/project/bible-app-29a53/hosting
- Firestore 데이터: https://console.firebase.google.com/project/bible-app-29a53/firestore
- 사용자 인증: https://console.firebase.google.com/project/bible-app-29a53/authentication

## 📊 사용 통계 확인

Firebase 콘솔에서 실시간으로 확인 가능:
- 동시 접속자 수
- 사용자 수
- Firestore 읽기/쓰기 횟수
- 호스팅 트래픽

## 🔄 업데이트 배포

코드 수정 후:

```bash
./deploy.sh
```

또는

```bash
npm run deploy
```

## ⚡ 빠른 명령어 모음

```bash
# 로그인
npx firebase login

# 배포
./deploy.sh

# 로그 확인
npx firebase hosting:channel:list

# 프로젝트 상태 확인
npx firebase projects:list
```

## 🎉 완료!

모든 설정이 완료되었습니다. 이제 핸드폰에서 바이브 앱을 마음껏 테스트하세요!

궁금한 점이 있으면 Firebase 콘솔이나 개발자 도구를 확인하세요.
